---
phase: 04-default-intrinsics-and-fisheye-defaults
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CMakeLists.txt
  - src/realtime/camera_models.h
  - src/realtime/camera_models.cpp
  - utils/derive_default_camera_intrinsics.cpp
  - utils/derive_default_camera_intrinsics.sh
  - tests/test_camera_models.cpp
autonomous: true
requirements:
  - DEF-04
  - DEF-05
  - DEF-06
must_haves:
  truths:
    - "Default intrinsics derive from one canonical `model + width + height + hfov` helper instead of repeated local formulas."
    - "The project utility wrapper reports the same `fx`, `fy`, `cx`, and `cy` values as the runtime helper for both supported camera models."
    - "Pinhole defaults use `90` degree horizontal FOV and fisheye defaults use `120` degree horizontal FOV unless the caller overrides `hfov`."
  artifacts:
    - path: "src/realtime/camera_models.cpp"
      provides: "Shared default-intrinsics derivation helper for `pinhole32` and `equi62_lut1d`"
    - path: "utils/derive_default_camera_intrinsics.cpp"
      provides: "Thin CLI wrapper for ad hoc default-intrinsics calculation"
    - path: "tests/test_camera_models.cpp"
      provides: "Formula-level regression coverage for default intrinsics"
  key_links:
    - from: "src/realtime/camera_models.cpp"
      to: "utils/derive_default_camera_intrinsics.cpp"
      via: "single-source default intrinsics derivation"
      pattern: "hfov|default intrinsics|fx|fy|cx|cy"
    - from: "src/realtime/camera_models.cpp"
      to: "tests/test_camera_models.cpp"
      via: "default intrinsics regression coverage"
      pattern: "pinhole32|equi62_lut1d|90|120"
---

<objective>
Introduce the single source of truth for default camera intrinsics and expose it through a small utility wrapper.

Purpose: satisfy DEF-04, DEF-05, and DEF-06 before any caller starts switching default models.
Output: one reusable C++ helper that derives `fx`, `fy`, `cx`, and `cy` from camera model, resolution, and horizontal FOV, plus a CLI/script wrapper and regression tests proving pinhole-90 and equi-120 defaults.
</objective>

<execution_context>
@/home/huangkai/.codex/get-shit-done/workflows/execute-plan.md
@/home/huangkai/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-default-intrinsics-and-fisheye-defaults/04-CONTEXT.md
@.planning/phases/04-default-intrinsics-and-fisheye-defaults/04-PATTERNS.md
@src/realtime/camera_models.h
@src/realtime/camera_models.cpp
@CMakeLists.txt
@tests/test_camera_models.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the canonical default-intrinsics helper next to the camera-model seam</name>
  <read_first>
src/realtime/camera_models.h
src/realtime/camera_models.cpp
docs/reference/src-cam/cam_pinhole32.h
docs/reference/src-cam/cam_equi62_lut1d.h
  </read_first>
  <files>src/realtime/camera_models.h, src/realtime/camera_models.cpp</files>
  <action>Add a small shared data type and helper for default intrinsics derivation near the existing camera-model math, for example a `DefaultCameraIntrinsics` struct plus a function shaped like `derive_default_intrinsics(CameraModelType model, int width, int height, double hfov_deg)`. The helper must reject non-positive dimensions and non-positive horizontal FOV, compute `cx = width / 2` and `cy = height / 2`, and derive `fx` and `fy` from horizontal FOV using one canonical formula that supports both `pinhole32` and `equi62_lut1d`. Also add tiny named helpers or constants for the default horizontal FOV values: pinhole `90` and fisheye `120`.</action>
  <verify>
    <automated>cmake --build build --target test_camera_models -j4 && ctest --test-dir build -R '^test_camera_models$' --output-on-failure</automated>
  </verify>
  <acceptance_criteria>`test_camera_models` covers both supported models, rejects invalid dimensions/FOV, and directly asserts the default pinhole-90 and fisheye-120 derivations produce the expected `fx`, `fy`, `cx`, and `cy` values.</acceptance_criteria>
  <done>The repo has one canonical helper for default intrinsics derivation and it is regression-tested at the formula level.</done>
</task>

<task type="auto">
  <name>Task 2: Expose the same derivation through a thin utility wrapper</name>
  <read_first>
CMakeLists.txt
utils/render_realtime.cpp
src/realtime/camera_models.h
  </read_first>
  <files>CMakeLists.txt, utils/derive_default_camera_intrinsics.cpp, utils/derive_default_camera_intrinsics.sh</files>
  <action>Add a small utility entrypoint under `utils/` that parses `--model`, `--width`, `--height`, and `--hfov-deg`, calls the shared helper from Task 1, and prints `fx`, `fy`, `cx`, and `cy` in a stable machine-readable format. If a wrapper script is added, keep it thin: it should only dispatch to the compiled utility and must not contain a second copy of the math. Wire the utility into `CMakeLists.txt` so the validation command can build and run it directly.</action>
  <verify>
    <automated>cmake --build build --target derive_default_camera_intrinsics test_camera_models -j4 && ./build/derive_default_camera_intrinsics --model pinhole32 --width 640 --height 480 --hfov-deg 90 && ./build/derive_default_camera_intrinsics --model equi62_lut1d --width 640 --height 480 --hfov-deg 120 && ctest --test-dir build -R '^test_camera_models$' --output-on-failure</automated>
  </verify>
  <acceptance_criteria>The utility builds, the two smoke invocations print the same values expected by the tests, and there is no duplicated focal-length formula outside the shared helper.</acceptance_criteria>
  <done>Ad hoc default-intrinsics calculations use the same code path as runtime defaults.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| default intrinsics helper -> scene/viewer callers | All default camera constructors must consume one canonical intrinsics formula instead of drifting local copies |
| utility wrapper -> runtime helper | The CLI output must stay coupled to the runtime math |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | T | `src/realtime/camera_models.cpp` | mitigate | Put all default-intrinsics math in one helper and test both models directly |
| T-04-01-02 | I | utility wrapper | mitigate | Make the wrapper call the helper instead of re-implementing the formula |
| T-04-01-03 | D | helper inputs | mitigate | Reject non-positive dimensions and invalid horizontal FOV in helper-level tests |
</threat_model>

<verification>
Build and run the new utility plus `test_camera_models`, then confirm the helper remains the only place that converts `hfov` into default `fx/fy/cx/cy`.
</verification>

<success_criteria>
The repo has a single canonical default-intrinsics derivation path for `pinhole32` and `equi62_lut1d`, and both runtime callers and ad hoc CLI usage consume it without duplicating formulas.
</success_criteria>

<output>
After completion, create `.planning/phases/04-default-intrinsics-and-fisheye-defaults/04-01-SUMMARY.md`
</output>
