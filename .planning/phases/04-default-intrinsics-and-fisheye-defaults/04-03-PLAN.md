---
phase: 04-default-intrinsics-and-fisheye-defaults
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01-PLAN.md
  - 04-02-PLAN.md
files_modified:
  - src/realtime/viewer/four_camera_rig.cpp
  - tests/test_viewer_four_camera_rig.cpp
  - tests/test_viewer_quality_reference.cpp
  - tests/test_realtime_scene_factory.cpp
autonomous: true
requirements:
  - DEF-02
  - DEF-03
  - DEF-04
must_haves:
  truths:
    - "No-argument viewer default rigs now fabricate fisheye cameras through the shared default-intrinsics helper instead of a local pinhole block."
    - "Explicit authored pinhole and fisheye viewer cameras still pack, resize, and render correctly after the default switch."
    - "The remaining default-entry regressions stay green, proving the fisheye default does not break viewer CPU reference or runtime rig construction."
  artifacts:
    - path: "src/realtime/viewer/four_camera_rig.cpp"
      provides: "Viewer no-arg default rig switched to fisheye-first shared default camera construction"
    - path: "tests/test_viewer_four_camera_rig.cpp"
      provides: "Default viewer rig regression coverage for fisheye defaults and explicit authored overrides"
    - path: "tests/test_viewer_quality_reference.cpp"
      provides: "CPU reference regression coverage after the viewer default switch"
  key_links:
    - from: "src/realtime/viewer/four_camera_rig.cpp"
      to: "src/realtime/camera_models.cpp"
      via: "shared default intrinsics helper"
      pattern: "default|equi62_lut1d|pinhole32|hfov"
    - from: "src/realtime/viewer/four_camera_rig.cpp"
      to: "tests/test_viewer_quality_reference.cpp"
      via: "default rig -> CPU reference -> OptiX viewer regression"
      pattern: "make_default_viewer_rig|render_shared_scene_from_camera"
---

<objective>
Finish the Phase 4 default switch by moving viewer no-arg defaults and remaining default-entry regressions onto the shared fisheye-first path.

Purpose: complete DEF-02 and DEF-03 across viewer-facing defaults after builtin preset helpers already switched.
Output: viewer no-arg default rig uses shared fisheye defaults, and regressions prove explicit pinhole/equi authored cameras still work while the fallback/default path remains valid.
</objective>

<execution_context>
@/home/huangkai/.codex/get-shit-done/workflows/execute-plan.md
@/home/huangkai/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-default-intrinsics-and-fisheye-defaults/04-CONTEXT.md
@.planning/phases/04-default-intrinsics-and-fisheye-defaults/04-PATTERNS.md
@.planning/phases/04-default-intrinsics-and-fisheye-defaults/04-01-PLAN.md
@.planning/phases/04-default-intrinsics-and-fisheye-defaults/04-02-PLAN.md
@src/realtime/viewer/four_camera_rig.cpp
@src/realtime/realtime_scene_factory.cpp
@tests/test_viewer_four_camera_rig.cpp
@tests/test_viewer_quality_reference.cpp
@tests/test_realtime_scene_factory.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the viewer's local pinhole default with a shared fisheye default camera spec</name>
  <read_first>
src/realtime/viewer/four_camera_rig.cpp
src/realtime/camera_models.h
tests/test_viewer_four_camera_rig.cpp
  </read_first>
  <files>src/realtime/viewer/four_camera_rig.cpp, tests/test_viewer_four_camera_rig.cpp</files>
  <action>Replace `default_pinhole_viewer_camera_spec(...)` with a helper that fabricates a default `CameraSpec` through the shared Phase 4 intrinsics function using `CameraModelType::equi62_lut1d` and the default fisheye `120` HFOV. Keep the no-argument viewer rig API and all existing pose/yaw-offset math intact. Update `tests/test_viewer_four_camera_rig.cpp` so the no-argument/default rig now asserts fisheye model and expected derived intrinsics, while the existing spec-driven mixed-model coverage still proves explicit pinhole and equi authored cameras work.</action>
  <verify>
    <automated>cmake --build build --target test_viewer_four_camera_rig test_realtime_scene_factory -j4 && ctest --test-dir build -R '^(test_viewer_four_camera_rig|test_realtime_scene_factory)$' --output-on-failure</automated>
  </verify>
  <acceptance_criteria>The no-argument viewer rig now packs `equi62_lut1d` cameras by default, `four_camera_rig.cpp` no longer contains a hardcoded pinhole default block, and mixed-model/authored overrides continue to pass.</acceptance_criteria>
  <done>The viewer default-rig entrypoint is now aligned with the project-wide fisheye default.</done>
</task>

<task type="auto">
  <name>Task 2: Prove viewer CPU reference and remaining default-entry consumers stay valid after the switch</name>
  <read_first>
tests/test_viewer_quality_reference.cpp
tests/test_realtime_scene_factory.cpp
src/realtime/viewer/four_camera_rig.cpp
  </read_first>
  <files>tests/test_viewer_quality_reference.cpp, tests/test_realtime_scene_factory.cpp</files>
  <action>Adjust viewer CPU-reference and related default-entry regression tests so they accept the new fisheye default rig while continuing to exercise explicit pinhole and explicit equi authored cameras. Keep the existing invalid packed-camera rejection behavior unchanged. Use `test_realtime_scene_factory.cpp` to ensure scene-authored default cameras from Phase 4 plan 02 still align with runtime rig expectations after the viewer default-rig switch.</action>
  <verify>
    <automated>cmake --build build --target test_viewer_four_camera_rig test_viewer_quality_reference test_realtime_scene_factory -j4 && ctest --test-dir build -R '^(test_viewer_four_camera_rig|test_viewer_quality_reference|test_realtime_scene_factory)$' --output-on-failure</automated>
  </verify>
  <acceptance_criteria>`test_viewer_quality_reference`, `test_viewer_four_camera_rig`, and `test_realtime_scene_factory` all pass together with the new fisheye default rig, while explicit pinhole coverage remains in place and invalid camera rejection stays narrow.</acceptance_criteria>
  <done>Viewer-facing default consumers are locked to the new fisheye default without losing explicit pinhole support.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| no-arg viewer rig -> packed runtime rig | Viewer default construction must switch models without breaking existing pose, resize, or rig packing behavior |
| default rig -> CPU reference / OptiX regression | The fallback/default viewer path must continue producing valid renderable cameras after the model switch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | T | `src/realtime/viewer/four_camera_rig.cpp` | mitigate | Replace the local pinhole default block with shared fisheye-first camera construction while preserving pose/yaw logic |
| T-04-03-02 | R | viewer regression tests | mitigate | Update viewer default-rig and CPU-reference tests to assert the new default model explicitly |
| T-04-03-03 | D | default-entry runtime consumers | mitigate | Run the viewer/default-entry regression slice that covers rig packing, runtime factory, and CPU reference together |
</threat_model>

<verification>
Build and run the viewer/default-entry regression slice, then confirm the no-arg viewer rig now defaults to fisheye while explicit authored pinhole paths remain intact.
</verification>

<success_criteria>
The viewer's remaining implicit default-camera path now defaults to `equi62_lut1d`, and the repo still proves explicit pinhole and explicit equi authored cameras remain valid across viewer/runtime/reference consumers.
</success_criteria>

<output>
After completion, create `.planning/phases/04-default-intrinsics-and-fisheye-defaults/04-03-SUMMARY.md`
</output>
