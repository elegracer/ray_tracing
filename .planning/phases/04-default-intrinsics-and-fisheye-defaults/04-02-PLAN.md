---
phase: 04-default-intrinsics-and-fisheye-defaults
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01-PLAN.md
files_modified:
  - src/scene/shared_scene_builders.cpp
  - tests/test_shared_scene_builders.cpp
  - tests/test_realtime_scene_factory.cpp
autonomous: true
requirements:
  - DEF-02
  - DEF-03
must_haves:
  truths:
    - "Builtin helper-generated default cameras switch to `equi62_lut1d` without silently rewriting explicit pinhole authorship."
    - "Default CPU and realtime preset helpers derive their authored intrinsics from the shared Phase 4 helper rather than local `vfov` math."
    - "Brownfield callers can still explicitly request pinhole defaults where the code intentionally wants pinhole behavior."
  artifacts:
    - path: "src/scene/shared_scene_builders.cpp"
      provides: "Builtin default CPU/realtime camera helpers migrated to fisheye-first shared derivation"
    - path: "tests/test_shared_scene_builders.cpp"
      provides: "Builtin default-camera contract coverage for fisheye-first defaults and explicit pinhole preservation"
    - path: "tests/test_realtime_scene_factory.cpp"
      provides: "Default scene-camera rig regression coverage after helper migration"
  key_links:
    - from: "src/scene/shared_scene_builders.cpp"
      to: "src/realtime/camera_models.cpp"
      via: "shared default intrinsics derivation"
      pattern: "hfov|default|pinhole32|equi62_lut1d"
    - from: "src/scene/shared_scene_builders.cpp"
      to: "tests/test_realtime_scene_factory.cpp"
      via: "authored builtin preset -> runtime rig path"
      pattern: "default_camera_rig_for_scene|find_realtime_view_preset"
---

<objective>
Move builtin default scene/preset construction onto the shared fisheye-first default-intrinsics path while preserving explicit pinhole call sites.

Purpose: satisfy DEF-02 and DEF-03 for shared-scene generated defaults before viewer-specific defaults switch.
Output: builtin CPU and realtime preset helpers that derive authored camera data from the Phase 4 helper, with regressions proving helper-generated defaults are fisheye and explicit pinhole presets remain pinhole.
</objective>

<execution_context>
@/home/huangkai/.codex/get-shit-done/workflows/execute-plan.md
@/home/huangkai/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-default-intrinsics-and-fisheye-defaults/04-CONTEXT.md
@.planning/phases/04-default-intrinsics-and-fisheye-defaults/04-PATTERNS.md
@.planning/phases/04-default-intrinsics-and-fisheye-defaults/04-01-PLAN.md
@src/scene/shared_scene_builders.cpp
@src/scene/shared_scene_builders.h
@src/realtime/realtime_scene_factory.cpp
@tests/test_shared_scene_builders.cpp
@tests/test_realtime_scene_factory.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate builtin default camera helpers from local `vfov` math to shared `hfov` derivation</name>
  <read_first>
src/scene/shared_scene_builders.cpp
src/realtime/camera_models.h
.planning/phases/04-default-intrinsics-and-fisheye-defaults/04-CONTEXT.md
  </read_first>
  <files>src/scene/shared_scene_builders.cpp</files>
  <action>Refactor `make_cpu_camera(...)` and `make_realtime_view_preset(...)` so the default path no longer computes focal lengths inline from `vfov`. Introduce shared helper usage that authors `CameraSpec` from `model + width/height + hfov`, with implicit/default paths now choosing `CameraModelType::equi62_lut1d` and default fisheye `120` HFOV. Keep a narrow compatibility wrapper or explicit call shape for callers that still intentionally want a pinhole default, using the default pinhole `90` HFOV. Do not rewrite repo-owned YAML or explicit pinhole-authored preset declarations here.</action>
  <verify>
    <automated>cmake --build build --target test_shared_scene_builders test_realtime_scene_factory -j4 && ctest --test-dir build -R '^(test_shared_scene_builders|test_realtime_scene_factory)$' --output-on-failure</automated>
  </verify>
  <acceptance_criteria>`shared_scene_builders.cpp` no longer contains local default focal-length math for builtin presets, helper-generated defaults author `equi62_lut1d`, and explicit pinhole call sites still have an intentional path instead of inheriting fisheye silently.</acceptance_criteria>
  <done>Builtin default camera construction is now driven by the shared Phase 4 helper and defaults to fisheye.</done>
</task>

<task type="auto">
  <name>Task 2: Lock builtin default-switch and explicit-pinhole preservation in tests</name>
  <read_first>
tests/test_shared_scene_builders.cpp
tests/test_realtime_scene_factory.cpp
src/scene/shared_scene_builders.cpp
  </read_first>
  <files>tests/test_shared_scene_builders.cpp, tests/test_realtime_scene_factory.cpp</files>
  <action>Extend existing tests so they assert helper-generated builtin defaults now author `equi62_lut1d` with the expected default intrinsics, while explicitly authored pinhole builtin presets still remain `pinhole32`. Cover both the structural builder surface and the realtime runtime-rig consumer surface. Prefer asserting concrete builtins already in the catalog instead of adding new scenes unless needed for isolation.</action>
  <verify>
    <automated>cmake --build build --target test_shared_scene_builders test_realtime_scene_factory -j4 && ctest --test-dir build -R '^(test_shared_scene_builders|test_realtime_scene_factory)$' --output-on-failure</automated>
  </verify>
  <acceptance_criteria>The updated tests demonstrate both halves of the contract: implicit builtin defaults switch to fisheye, and explicit pinhole authored presets stay pinhole through the runtime factory path.</acceptance_criteria>
  <done>Phase 4 has direct regression proof for builtin default switching without pinhole regression of explicit presets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared default helper -> builtin preset authorship | Builtin scene defaults must switch models only through explicit helper behavior, not through hidden local formulas |
| builtin preset authorship -> runtime rig | Realtime consumers must still preserve explicit pinhole authorship after helper migration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | T | `src/scene/shared_scene_builders.cpp` | mitigate | Remove local default `vfov` derivation from helper-generated presets and route defaults through the shared helper |
| T-04-02-02 | T | explicit pinhole builtins | mitigate | Keep an intentional explicit pinhole helper path and assert it in builder/runtime tests |
| T-04-02-03 | R | builtin preset -> runtime rig | mitigate | Extend `test_realtime_scene_factory` to prove authored defaults survive into packed runtime cameras |
</threat_model>

<verification>
Build and run `test_shared_scene_builders` and `test_realtime_scene_factory`, then confirm helper-generated builtin defaults author fisheye while explicit pinhole presets still pack as pinhole.
</verification>

<success_criteria>
All builtin default camera construction funnels through the shared default-intrinsics helper, defaults to fisheye, and preserves explicit pinhole authorship where the code intentionally asks for it.
</success_criteria>

<output>
After completion, create `.planning/phases/04-default-intrinsics-and-fisheye-defaults/04-02-SUMMARY.md`
</output>
