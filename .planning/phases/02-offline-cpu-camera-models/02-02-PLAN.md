---
phase: 02-offline-cpu-camera-models
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/core/offline_shared_scene_renderer.cpp
  - tests/test_offline_shared_scene_renderer.cpp
  - tests/test_shared_scene_regression.cpp
autonomous: true
requirements:
  - CAM-02
must_haves:
  truths:
    - "`render_shared_scene(...)` honors the selected camera model from shared-scene CPU presets instead of ignoring `camera.model`."
    - "`render_shared_scene_from_camera(...)` and preset-based offline rendering use the same model-aware render path."
    - "Changing a camera between `pinhole32` and `equi62_lut1d` changes offline output without introducing a separate scene format or offline-only declaration."
  artifacts:
    - path: "src/core/offline_shared_scene_renderer.cpp"
      provides: "Unified preset-path and explicit-camera offline renderer adapter"
    - path: "tests/test_offline_shared_scene_renderer.cpp"
      provides: "Shared-scene offline integration checks for model switching"
    - path: "tests/test_shared_scene_regression.cpp"
      provides: "Structural proof that shared-scene CPU presets still preserve authored camera payloads"
  key_links:
    - from: "src/scene/shared_scene_builders.h"
      to: "src/core/offline_shared_scene_renderer.cpp"
      via: "CpuCameraPreset.camera + pose fields"
      pattern: "CameraSpec|lookfrom|lookat|vup"
    - from: "src/core/offline_shared_scene_renderer.cpp"
      to: "tests/test_offline_shared_scene_renderer.cpp"
      via: "shared-scene preset path and explicit packed-camera path"
      pattern: "render_shared_scene\\(|render_shared_scene_from_camera\\("
---

<objective>
Wire the shared-scene offline entrypoints through the new model-aware seam.

Purpose: satisfy D-04, D-05, and D-06 by making both offline render entrypoints consume canonical camera model/intrinsics data while keeping outer preset pose fields as pose-only inputs.
Output: one unified offline render path used by both `render_shared_scene(...)` and `render_shared_scene_from_camera(...)`, plus shared-scene integration coverage that proves preset camera model changes actually affect offline output.
</objective>

<execution_context>
@/home/huangkai/.codex/get-shit-done/workflows/execute-plan.md
@/home/huangkai/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-offline-cpu-camera-models/02-CONTEXT.md
@.planning/phases/02-offline-cpu-camera-models/02-RESEARCH.md
@.planning/phases/02-offline-cpu-camera-models/02-01-PLAN.md
@src/scene/shared_scene_builders.h
@src/core/offline_shared_scene_renderer.h
@src/core/offline_shared_scene_renderer.cpp
@tests/test_offline_shared_scene_renderer.cpp
@tests/test_shared_scene_regression.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route both offline entrypoints through canonical camera data</name>
  <read_first>
src/core/offline_shared_scene_renderer.cpp
src/scene/shared_scene_builders.h
.planning/phases/02-offline-cpu-camera-models/02-CONTEXT.md
  </read_first>
  <files>src/core/offline_shared_scene_renderer.cpp</files>
  <action>Rework the offline shared-scene adapter so `render_shared_scene(...)` and `render_shared_scene_from_camera(...)` both feed the same model-aware renderer introduced in Plan 01. For the preset path, synthesize the offline camera state from `CpuCameraPreset.camera` plus the outer pose fields (`lookfrom`, `lookat`, `vup`) and pinhole-only DoF fields (`defocus_angle`, `focus_dist`). The canonical camera payload must define model and intrinsics; the outer preset fields must only provide pose and pinhole DoF policy. Keep the existing sample-count override rule and canonical scene-content lookup semantics unchanged.</action>
  <verify>
    <automated>cmake --build build --target test_offline_shared_scene_renderer test_shared_scene_regression -j4 && ctest --test-dir build -R '^(test_offline_shared_scene_renderer|test_shared_scene_regression)$' --output-on-failure</automated>
  </verify>
  <acceptance_criteria>`render_shared_scene(...)` and `render_shared_scene_from_camera(...)` share the same model-aware adapter path, the preset route no longer ignores `camera.model`, and the targeted offline/shared-scene regression slice remains green.</acceptance_criteria>
  <done>Both offline entrypoints now consume canonical camera data through a single render path, with preset pose fields reduced to pose-only input.</done>
</task>

<task type="auto">
  <name>Task 2: Prove shared-scene model switching changes offline output</name>
  <read_first>
tests/test_offline_shared_scene_renderer.cpp
tests/test_shared_scene_regression.cpp
src/scene/scene_file_catalog.cpp
  </read_first>
  <files>tests/test_offline_shared_scene_renderer.cpp, tests/test_shared_scene_regression.cpp</files>
  <action>Add end-to-end integration coverage that proves changing a camera between `pinhole32` and `equi62_lut1d` changes the offline result through the same shared-scene path. Prefer existing repo patterns: use `render_shared_scene_from_camera(...)` for direct model-switch output comparisons and, if needed, reuse the temp-root `SceneFileCatalog` test pattern to author a minimal file-backed fisheye CPU preset and assert `render_shared_scene(...)` honors it. Keep the checks deterministic and small-sample; the goal is to prove model selection is wired, not to create a high-cost image-quality benchmark.</action>
  <verify>
    <automated>cmake --build build --target test_offline_shared_scene_renderer test_shared_scene_regression test_scene_file_catalog -j4 && ctest --test-dir build -R '^(test_offline_shared_scene_renderer|test_shared_scene_regression|test_scene_file_catalog)$' --output-on-failure</automated>
  </verify>
  <acceptance_criteria>The shared-scene offline tests now demonstrate that toggling the selected camera model changes offline output through the same canonical scene pipeline, and any temp-catalog file-backed coverage reuses the established scene-file test pattern instead of mutating repo-owned assets.</acceptance_criteria>
  <done>Phase 2 has explicit integration proof that shared-scene offline rendering honors camera model choice for both explicit packed cameras and preset-authored scene renders.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared-scene CPU presets -> offline renderer | Authored preset camera data must become the real offline camera contract without reintroducing legacy pinhole-only semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | T | `src/core/offline_shared_scene_renderer.cpp` | mitigate | Build one canonical offline camera adapter for both public entrypoints so explicit packed-camera and preset-based rendering cannot diverge |
| T-02-02-02 | R | `render_shared_scene(...)` preset path | mitigate | Add integration coverage that exercises a fisheye-authored CPU preset through the shared-scene route, not only through explicit packed-camera rendering |
| T-02-02-03 | D | temp file-backed scene tests | accept | Temp catalog roots may add test complexity, but they follow an existing repo pattern and avoid changing repo-owned assets for one phase-local proof |
</threat_model>

<verification>
Run the shared-scene offline regression slice and verify that tests cover both explicit packed-camera rendering and preset-authored offline rendering.
</verification>

<success_criteria>
Offline shared-scene rendering consumes the selected camera model from canonical camera data for both public entrypoints, and switching between pinhole and fisheye changes offline output through the same scene pipeline.
</success_criteria>

<output>
After completion, create `.planning/phases/02-offline-cpu-camera-models/02-02-SUMMARY.md`
</output>
