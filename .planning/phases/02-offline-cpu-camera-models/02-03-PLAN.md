---
phase: 02-offline-cpu-camera-models
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - tests/test_viewer_quality_reference.cpp
  - tests/test_offline_shared_scene_renderer.cpp
autonomous: true
requirements:
  - CAM-02
must_haves:
  truths:
    - "The explicit packed-camera CPU reference path remains reliable after the offline model-aware rollout."
    - "Phase 2 keeps direction-level and image-level regressions aligned instead of relying on smoke output only."
    - "Offline camera-model support lands without silently invalidating existing viewer-reference workflows."
  artifacts:
    - path: "tests/test_viewer_quality_reference.cpp"
      provides: "Consumer-facing regression coverage for the explicit CPU reference renderer"
    - path: "tests/test_offline_shared_scene_renderer.cpp"
      provides: "Phase-level image and contract coverage kept in sync with the viewer consumer"
  key_links:
    - from: "src/core/offline_shared_scene_renderer.h"
      to: "tests/test_viewer_quality_reference.cpp"
      via: "explicit packed-camera CPU reference contract"
      pattern: "render_shared_scene_from_camera"
    - from: "tests/test_offline_shared_scene_renderer.cpp"
      to: "tests/test_viewer_quality_reference.cpp"
      via: "shared expectation that the CPU reference renderer is model-aware"
      pattern: "pinhole32|equi62_lut1d"
---

<objective>
Stabilize downstream consumer regressions after the offline camera-model rollout.

Purpose: finish the Phase 2 regression story by updating the explicit CPU reference consumer path and ensuring seam-level and consumer-level tests describe the same supported contract.
Output: updated viewer-reference regressions plus synchronized offline renderer tests that make future Phase 3/4 work safer.
</objective>

<execution_context>
@/home/huangkai/.codex/get-shit-done/workflows/execute-plan.md
@/home/huangkai/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-offline-cpu-camera-models/02-CONTEXT.md
@.planning/phases/02-offline-cpu-camera-models/02-RESEARCH.md
@.planning/phases/02-offline-cpu-camera-models/02-02-PLAN.md
@src/core/offline_shared_scene_renderer.h
@tests/test_viewer_quality_reference.cpp
@tests/test_offline_shared_scene_renderer.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile viewer CPU-reference tests with the new offline camera contract</name>
  <read_first>
tests/test_viewer_quality_reference.cpp
src/core/offline_shared_scene_renderer.h
.planning/phases/02-offline-cpu-camera-models/02-CONTEXT.md
  </read_first>
  <files>tests/test_viewer_quality_reference.cpp</files>
  <action>Update `test_viewer_quality_reference.cpp` so it reflects the Phase 2 offline camera contract instead of the old pinhole-only assumption. Keep the role of `render_shared_scene_from_camera(...)` as the explicit CPU reference path for viewer-quality comparisons, but revise any assertions that only codified the previous pinhole-only guard behavior. The end state should clearly document what remains intentionally unsupported after Phase 2, while proving the CPU reference renderer stays usable for the viewer-quality workflow.</action>
  <verify>
    <automated>cmake --build build --target test_viewer_quality_reference -j4 && ctest --test-dir build -R '^test_viewer_quality_reference$' --output-on-failure</automated>
  </verify>
  <acceptance_criteria>`test_viewer_quality_reference` passes and its assertions describe the post-Phase-2 CPU reference contract explicitly rather than implicitly assuming the old pinhole-only behavior.</acceptance_criteria>
  <done>The primary downstream consumer of `render_shared_scene_from_camera(...)` is aligned with the new offline camera-model rollout.</done>
</task>

<task type="auto">
  <name>Task 2: Keep phase-level regressions synchronized with the consumer contract</name>
  <read_first>
tests/test_offline_shared_scene_renderer.cpp
tests/test_viewer_quality_reference.cpp
.planning/phases/02-offline-cpu-camera-models/02-VALIDATION.md
  </read_first>
  <files>tests/test_offline_shared_scene_renderer.cpp, tests/test_viewer_quality_reference.cpp</files>
  <action>Finish Phase 2 by aligning the offline shared-scene regression slice with the viewer-reference consumer contract. Ensure the phase-level tests cover the same supported camera-model behaviors that the viewer-reference test expects, so future phases do not have to guess whether a failure is a camera-math bug or a stale CPU-reference assumption. Prefer small, explicit checks over adding more heavy image-comparison scenarios.</action>
  <verify>
    <automated>cmake --build build --target test_camera_models test_offline_shared_scene_renderer test_viewer_quality_reference -j4 && ctest --test-dir build -R '^(test_camera_models|test_offline_shared_scene_renderer|test_viewer_quality_reference)$' --output-on-failure</automated>
  </verify>
  <acceptance_criteria>The combined regression slice for shared math, offline shared-scene rendering, and viewer CPU-reference behavior passes together, and the tests describe one coherent offline camera-model contract.</acceptance_criteria>
  <done>Phase 2 closes with synchronized seam-level, integration-level, and consumer-level regression coverage for the offline camera-model path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| offline renderer -> viewer CPU reference consumer | The explicit CPU reference renderer is relied on by viewer-quality tests and must stay trustworthy after the contract broadens |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | R | `tests/test_viewer_quality_reference.cpp` | mitigate | Update the consumer test deliberately so it encodes the new contract instead of silently preserving stale pinhole-only assumptions |
| T-02-03-02 | T | phase-level vs consumer-level tests | mitigate | Run the shared-math, offline-renderer, and viewer-reference regression slice together so mismatched expectations are caught before verify-work |
</threat_model>

<verification>
Build and run the combined camera-model / offline-renderer / viewer-reference regression slice.
</verification>

<success_criteria>
The explicit packed-camera CPU reference path remains a valid downstream consumer after the offline camera-model rollout, and the phase's regression tests all agree on the same supported contract.
</success_criteria>

<output>
After completion, create `.planning/phases/02-offline-cpu-camera-models/02-03-SUMMARY.md`
</output>
