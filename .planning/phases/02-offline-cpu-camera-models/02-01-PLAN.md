---
phase: 02-offline-cpu-camera-models
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/common/camera.h
  - src/core/offline_shared_scene_renderer.cpp
  - tests/test_offline_shared_scene_renderer.cpp
autonomous: true
requirements:
  - CAM-02
must_haves:
  truths:
    - "Offline CPU ray generation uses shared pinhole/equi camera math instead of the old pinhole-only viewport path."
    - "The existing CPU tracer loop is preserved; only the primary-ray seam changes."
    - "Pinhole retains depth of field while equi stays rectangular and no-defocus."
  artifacts:
    - path: "src/common/camera.h"
      provides: "Model-aware offline primary-ray emission without replacing the tracer"
    - path: "src/core/offline_shared_scene_renderer.cpp"
      provides: "Canonical adapter from packed/shared camera data into the offline render path"
    - path: "tests/test_offline_shared_scene_renderer.cpp"
      provides: "Offline camera-model contract checks"
  key_links:
    - from: "src/realtime/camera_models.cpp"
      to: "src/common/camera.h"
      via: "shared pinhole/equi unprojection math"
      pattern: "unproject_(pinhole32|equi62_lut1d)"
    - from: "src/core/offline_shared_scene_renderer.cpp"
      to: "src/common/camera.h"
      via: "model-aware camera configuration"
      pattern: "PackedCamera|CameraModelType"
---

<objective>
Create the offline primary-ray generation seam that Phase 2 needs, without forking the CPU path tracer.

Purpose: satisfy D-02, D-03, D-07, and D-08 by making the offline renderer emit rays from canonical camera model math while preserving the existing tracing/sampling loop.
Output: a model-aware offline camera path that accepts canonical camera data for both supported models and keeps pinhole-only defocus behavior explicit.
</objective>

<execution_context>
@/home/huangkai/.codex/get-shit-done/workflows/execute-plan.md
@/home/huangkai/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-offline-cpu-camera-models/02-CONTEXT.md
@.planning/phases/02-offline-cpu-camera-models/02-RESEARCH.md
@.planning/phases/02-offline-cpu-camera-models/02-PATTERNS.md
@src/common/camera.h
@src/core/offline_shared_scene_renderer.cpp
@src/realtime/camera_models.h
@src/realtime/camera_models.cpp

<interfaces>
From `src/common/camera.h` today:
```cpp
class Camera {
public:
    void render(const pro::proxy<Hittable>& world, const pro::proxy<Hittable>& lights = {});

private:
    Ray get_ray(int x, int y, int s_x, int s_y) const;
};
```

From `src/realtime/camera_models.h`:
```cpp
Eigen::Vector3d unproject_pinhole32(const Pinhole32Params& params, const Eigen::Vector2d& pixel);
Eigen::Vector3d unproject_equi62_lut1d(const Equi62Lut1DParams& params, const Eigen::Vector2d& pixel);
```

From `src/realtime/camera_rig.h`:
```cpp
struct PackedCamera {
    int width = 0;
    int height = 0;
    CameraModelType model = CameraModelType::pinhole32;
    Sophus::SE3d T_rc {};
    Pinhole32Params pinhole{};
    Equi62Lut1DParams equi{};
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a model-aware offline ray-emission seam</name>
  <read_first>
src/common/camera.h
src/realtime/camera_models.cpp
.planning/phases/02-offline-cpu-camera-models/02-CONTEXT.md
  </read_first>
  <files>src/common/camera.h, src/core/offline_shared_scene_renderer.cpp</files>
  <action>Refactor the offline camera path so the CPU tracer can emit primary rays from canonical camera model data instead of only from the legacy pinhole viewport computed from `vfov`. Keep `Camera::render(...)`, `ray_color(...)`, sampling, and image assembly intact; only replace or extend the logic that converts pixel coordinates to a primary ray. Use the existing shared math in `unproject_pinhole32(...)` and `unproject_equi62_lut1d(...)` to construct the local camera ray, then transform it with the authored/runtime pose carried by the offline camera state. Preserve pinhole depth of field using the current defocus disk only for `pinhole32`; for `equi62_lut1d`, keep the full rectangular output and emit no-defocus rays per D-01 and D-08. Do not introduce a second tracer loop or a separate offline-only fisheye math implementation.</action>
  <verify>
    <automated>cmake --build build --target test_camera_models test_offline_shared_scene_renderer -j4 && ctest --test-dir build -R '^(test_camera_models|test_offline_shared_scene_renderer)$' --output-on-failure</automated>
  </verify>
  <acceptance_criteria>`test_camera_models` and `test_offline_shared_scene_renderer` both pass, and `rg -n "unproject_pinhole32|unproject_equi62_lut1d|CameraModelType" src/common/camera.h src/core/offline_shared_scene_renderer.cpp` shows the offline path now depends on shared camera math rather than only `vfov` viewport construction.</acceptance_criteria>
  <done>The offline renderer has a single model-aware primary-ray seam that supports both `pinhole32` and `equi62_lut1d` while preserving the existing path-tracing loop.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the Phase 2 camera contract at the offline seam</name>
  <read_first>
src/core/offline_shared_scene_renderer.cpp
tests/test_offline_shared_scene_renderer.cpp
.planning/phases/02-offline-cpu-camera-models/02-CONTEXT.md
  </read_first>
  <files>src/core/offline_shared_scene_renderer.cpp, tests/test_offline_shared_scene_renderer.cpp</files>
  <action>Update the offline camera contract tests so Phase 2 explicitly verifies the locked semantics: pixel-center rays for representative pinhole and fisheye cameras agree with the shared camera math, fisheye renders keep rectangular image output, and pinhole still uses the current depth-of-field policy while fisheye does not invent one. Keep these checks focused on the seam you introduced in Task 1; avoid bundling preset-path verification or viewer-reference regressions here.</action>
  <verify>
    <automated>cmake --build build --target test_offline_shared_scene_renderer -j4 && ctest --test-dir build -R '^test_offline_shared_scene_renderer$' --output-on-failure</automated>
  </verify>
  <acceptance_criteria>`test_offline_shared_scene_renderer` asserts the new seam-level behavior for both supported models, and the test output no longer depends on the previous pinhole-only rejection path as the primary contract.</acceptance_criteria>
  <done>Phase 2 has explicit seam-level regression coverage for model-aware offline ray generation and the locked no-mask / pinhole-DoF-only policy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared camera math -> offline tracer | Canonical model/intrinsics cross into the legacy CPU renderer and must not be reinterpreted by a second math path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | T | `src/common/camera.h` | mitigate | Reuse `unproject_pinhole32(...)` / `unproject_equi62_lut1d(...)` directly so offline ray generation cannot drift from shared camera math |
| T-02-01-02 | D | `src/common/camera.h` | mitigate | Keep pinhole defocus explicit and gated by camera model so fisheye does not silently inherit an invalid blur model |
| T-02-01-03 | R | `src/core/offline_shared_scene_renderer.cpp` | accept | Public entrypoints remain unchanged; behavior changes are guarded by seam-level tests and later integration regressions |
</threat_model>

<verification>
Build and run `test_camera_models` plus the offline shared-scene test slice, then grep the offline seam for shared camera-model helper usage.
</verification>

<success_criteria>
The offline CPU path emits primary rays from canonical shared camera math for both supported models, keeps pinhole depth of field only, and does so without forking the tracer loop.
</success_criteria>

<output>
After completion, create `.planning/phases/02-offline-cpu-camera-models/02-01-SUMMARY.md`
</output>
