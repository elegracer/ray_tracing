---
phase: 01-shared-camera-schema
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/scene/shared_scene_builders.h
  - src/scene/scene_definition.h
  - src/scene/shared_scene_builders.cpp
  - src/realtime/realtime_scene_factory.cpp
  - src/core/offline_shared_scene_renderer.cpp
  - tests/test_realtime_scene_factory.cpp
autonomous: true
requirements:
  - CAM-01
  - CAM-05
  - DEF-01
must_haves:
  truths:
    - "Builtin scene presets author camera model and common intrinsics through CameraSpec instead of vfov-only camera fields."
    - "Offline and realtime preset consumers adapt from CameraSpec while preserving current renderer behavior for pinhole-authored content."
    - "Every repo-owned builtin camera declaration explicitly sets `model` and zero-distortion slots."
  artifacts:
    - path: "src/scene/shared_scene_builders.h"
      provides: "Preset structs that embed CameraSpec as the canonical camera payload"
    - path: "src/scene/shared_scene_builders.cpp"
      provides: "Builtin scene/preset declarations migrated to explicit model-based camera specs"
    - path: "src/realtime/realtime_scene_factory.cpp"
      provides: "Realtime rig creation adapted from CameraSpec"
  key_links:
    - from: "src/scene/shared_scene_builders.cpp"
      to: "src/realtime/realtime_scene_factory.cpp"
      via: "RealtimeViewPreset.camera"
      pattern: "camera\\.model|add_camera\\("
    - from: "src/scene/shared_scene_builders.h"
      to: "src/core/offline_shared_scene_renderer.cpp"
      via: "CpuCameraPreset camera payload"
      pattern: "camera\\."
---

<objective>
Move builtin scene ownership onto CameraSpec without changing Phase 2/3 renderer math scope.

Purpose: Satisfy D-01, D-03, D-04, D-05, and D-08 for builtin C++ scene content while keeping current pinhole-only offline behavior and current realtime projection code intact.
Output: CameraSpec-backed preset structs, migrated builtin scene declarations, and consumers updated to read the shared schema.
</objective>

<execution_context>
@/home/huangkai/.codex/get-shit-done/workflows/execute-plan.md
@/home/huangkai/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-shared-camera-schema/01-CONTEXT.md
@.planning/phases/01-shared-camera-schema/01-RESEARCH.md
@.planning/phases/01-shared-camera-schema/01-01-PLAN.md
@src/scene/camera_spec.h
@src/scene/shared_scene_builders.h
@src/scene/scene_definition.h
@src/scene/shared_scene_builders.cpp
@src/realtime/realtime_scene_factory.cpp
@src/core/offline_shared_scene_renderer.cpp

<interfaces>
From `src/scene/shared_scene_builders.h` today:
```cpp
struct CpuCameraPreset {
    double aspect_ratio;
    int image_width;
    int max_depth;
    double vfov;
    Eigen::Vector3d lookfrom;
    Eigen::Vector3d lookat;
    Eigen::Vector3d vup;
    double defocus_angle;
    double focus_dist;
};

struct RealtimeViewPreset {
    viewer::BodyPose initial_body_pose;
    viewer::ViewerFrameConvention frame_convention;
    double vfov_deg;
    bool use_default_viewer_intrinsics;
    double base_move_speed;
};
```

From `src/realtime/realtime_scene_factory.cpp` today:
```cpp
CameraRig make_camera_rig_from_preset(const scene::RealtimeViewPreset& preset, int camera_count, int width, int height) {
    const Pinhole32Params pinhole = preset.use_default_viewer_intrinsics
        ? make_default_viewer_pinhole(width, height)
        : make_pinhole_from_vfov(preset.vfov_deg, width, height);
    ...
    rig.add_pinhole(pinhole, T_bc, width, height);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Embed CameraSpec into authored preset structs and consumer shims</name>
  <read_first>
src/scene/camera_spec.h
src/scene/shared_scene_builders.h
src/scene/scene_definition.h
src/realtime/realtime_scene_factory.cpp
src/core/offline_shared_scene_renderer.cpp
  </read_first>
  <files>src/scene/shared_scene_builders.h, src/scene/scene_definition.h, src/realtime/realtime_scene_factory.cpp, src/core/offline_shared_scene_renderer.cpp, tests/test_realtime_scene_factory.cpp</files>
  <action>Per D-01 and D-03, change the shared authored preset types so `CpuCameraPreset` and `RealtimeViewPreset` carry `CameraSpec camera` as the canonical camera payload. Keep only non-schema outer fields that Phase 1 still needs for current renderer behavior, such as offline pose/framing (`lookfrom`, `lookat`, `vup`, `aspect_ratio`, `image_width`, `max_depth`, `defocus_angle`, `focus_dist`) and realtime spawn/move metadata (`initial_body_pose`, `frame_convention`, `base_move_speed`). Remove long-lived authored `vfov_deg` and `use_default_viewer_intrinsics`. In `src/realtime/realtime_scene_factory.cpp`, treat `preset.camera.width` and `preset.camera.height` as authored calibration resolution, not a hard runtime output lock: when `default_camera_rig_for_scene(..., width, height)` receives the same dimensions, preserve authored `fx`, `fy`, `cx`, and `cy` exactly; when it receives different runtime dimensions, compute proportional `scale_x = width / preset.camera.width` and `scale_y = height / preset.camera.height`, rescale `fx`, `fy`, `cx`, and `cy` to the requested output size, set the runtime rig slot width/height to the requested dimensions, and leave the authored preset data unchanged. Compose `initial_body_pose` with `preset.camera.T_bc` and push the runtime-sized camera through `CameraRig::add_camera(...)`. Keep `src/core/offline_shared_scene_renderer.cpp` behavior pinhole-only per phase boundary: it may still configure the legacy offline `Camera` from pose/framing fields, but it must stop treating `vfov`-only structs as the canonical shared camera schema. Update `tests/test_realtime_scene_factory.cpp` in this task so it verifies both same-size preservation and resized-output proportional rescaling for builtin presets.</action>
  <verify>
    <automated>cmake --build build --target test_realtime_scene_factory test_offline_shared_scene_renderer -j4 && ctest --test-dir build -R '^(test_realtime_scene_factory|test_offline_shared_scene_renderer)$' --output-on-failure</automated>
  </verify>
  <acceptance_criteria>`rg -n "CameraSpec camera" src/scene/shared_scene_builders.h src/scene/scene_definition.h` finds the canonical payload in both authored preset layers, `tests/test_realtime_scene_factory.cpp` covers both same-size preservation and resized-output proportional rescaling for `fx/fy/cx/cy`, and `ctest --test-dir build -R '^(test_realtime_scene_factory|test_offline_shared_scene_renderer)$' --output-on-failure` passes without adding fisheye renderer behavior.</acceptance_criteria>
  <done>The shared scene layer owns camera schema via `CameraSpec`, and both offline/realtime consumers compile against that ownership boundary while preserving current renderer semantics, including the explicit authored-resolution-to-runtime-resize rule for realtime rig generation.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate builtin scene preset declarations to explicit CameraSpec authoring</name>
  <read_first>
src/scene/shared_scene_builders.cpp
src/scene/shared_scene_builders.h
src/realtime/realtime_scene_factory.cpp
tests/test_realtime_scene_factory.cpp
  </read_first>
  <files>src/scene/shared_scene_builders.cpp, tests/test_realtime_scene_factory.cpp</files>
  <action>Replace `make_cpu_camera(...)` and `make_realtime_view_preset(...)` helpers with helpers that author explicit `CameraSpec` values per D-04, D-05, and D-08. Every builtin CPU and realtime camera declaration in `kCpuPresetRegistry` and `kRealtimePresetRegistry` must set `model`, `width`, `height`, `fx`, `fy`, `cx`, `cy`, identity `camera.T_bc`, and zeroed model-specific parameter slots. Preserve current framing by encoding the same pinhole intrinsics previously implied by `vfov` or `use_default_viewer_intrinsics`; do not introduce fisheye defaults, offline fisheye rendering, or the Phase 4 intrinsics utility. Update `tests/test_realtime_scene_factory.cpp` to assert the rig uses explicit model/intrinsics from builtin presets rather than legacy authored `vfov` fields.</action>
  <verify>
    <automated>cmake --build build --target test_realtime_scene_factory test_offline_shared_scene_renderer -j4 && ctest --test-dir build -R '^(test_realtime_scene_factory|test_offline_shared_scene_renderer)$' --output-on-failure</automated>
  </verify>
  <acceptance_criteria>`rg -n "CameraModelType::|\\.fx =|\\.fy =|\\.cx =|\\.cy =|\\.T_bc =" src/scene/shared_scene_builders.cpp` shows explicit builtin camera authoring, and the targeted realtime/offline structural tests stay green.</acceptance_criteria>
  <done>All builtin project-owned camera declarations are explicit `CameraSpec` instances with `model` present, zero distortion defaults, and preserved current pinhole framing behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| builtin scene tables -> shared preset structs | Repo-authored scene data becomes the canonical source for downstream CPU/realtime camera setup |
| shared preset structs -> offline/realtime consumers | Consumers must preserve schema ownership and not reintroduce hidden camera defaults |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | T | `src/scene/shared_scene_builders.cpp` | mitigate | Encode explicit `model`, common intrinsics, and zero-distortion slots in every builtin camera declaration so later default switches cannot silently rewrite camera semantics |
| T-01-02-02 | D | `src/realtime/realtime_scene_factory.cpp` | mitigate | Adapt from `preset.camera` and keep current projection math path unchanged; do not add Phase 3 model-switching behavior or Phase 4 default-intrinsics generation here |
| T-01-02-03 | R | `src/core/offline_shared_scene_renderer.cpp` | accept | Offline rendering remains pinhole-only in this phase by design; regression tests document that boundary until Phase 2 changes it intentionally |
</threat_model>

<verification>
Rebuild and run the offline/realtime structural tests for preset consumption, then grep builtin registry code for explicit camera model/intrinsics fields.
</verification>

<success_criteria>
Builtin scene definitions author cameras through `CameraSpec`, every builtin camera declaration explicitly sets `model`, and current offline/realtime preset consumers stay green without leaking later-phase rendering work into this phase.
</success_criteria>

<output>
After completion, create `.planning/phases/01-shared-camera-schema/01-02-SUMMARY.md`
</output>
