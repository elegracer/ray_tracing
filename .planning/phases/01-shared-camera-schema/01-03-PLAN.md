---
phase: 01-shared-camera-schema
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/scene/yaml_scene_loader.cpp
  - assets/scenes/cornell_box/scene.yaml
  - assets/scenes/final_room/scene.yaml
  - assets/scenes/imported_obj_smoke/scene.yaml
  - assets/scenes/simple_light/scene.yaml
  - tests/test_yaml_scene_loader.cpp
  - tests/test_shared_scene_regression.cpp
autonomous: true
requirements:
  - CAM-01
  - CAM-05
  - DEF-01
must_haves:
  truths:
    - "YAML-backed scene configs declare camera model and common intrinsics explicitly for every repo-owned camera."
    - "The YAML loader populates CameraSpec and rejects missing model or legacy-only camera declarations."
    - "Builtin and YAML-backed catalog paths preserve the same model/intrinsics data for downstream consumers."
  artifacts:
    - path: "src/scene/yaml_scene_loader.cpp"
      provides: "CameraSpec-based YAML parsing and validation"
    - path: "assets/scenes/cornell_box/scene.yaml"
      provides: "Repo-owned explicit camera schema example"
    - path: "tests/test_shared_scene_regression.cpp"
      provides: "Catalog and YAML structural regression coverage for migrated camera declarations"
  key_links:
    - from: "assets/scenes/final_room/scene.yaml"
      to: "src/scene/yaml_scene_loader.cpp"
      via: "explicit camera model/intrinsics parsing"
      pattern: "model:|fx:|fy:|cx:|cy:"
    - from: "src/scene/yaml_scene_loader.cpp"
      to: "tests/test_shared_scene_regression.cpp"
      via: "file-backed scene definition and catalog overlay coverage"
      pattern: "camera\\.model|camera\\.fx|camera\\.fy"
---

<objective>
Migrate YAML-authored camera declarations and structural regressions onto the canonical schema.

Purpose: Finish D-04, D-05, D-08, and D-09 by making repo-owned YAML scenes and loader validation use the same explicit CameraSpec schema as builtin scenes.
Output: CameraSpec-aware YAML parsing, repo-owned scene file migration, and structural regression coverage that proves builtin/file-backed equivalence.
</objective>

<execution_context>
@/home/huangkai/.codex/get-shit-done/workflows/execute-plan.md
@/home/huangkai/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-shared-camera-schema/01-CONTEXT.md
@.planning/phases/01-shared-camera-schema/01-RESEARCH.md
@.planning/phases/01-shared-camera-schema/01-02-PLAN.md
@src/scene/camera_spec.h
@src/scene/yaml_scene_loader.cpp
@assets/scenes/cornell_box/scene.yaml
@assets/scenes/final_room/scene.yaml
@assets/scenes/imported_obj_smoke/scene.yaml
@assets/scenes/simple_light/scene.yaml
@tests/test_yaml_scene_loader.cpp
@tests/test_shared_scene_regression.cpp

<interfaces>
From `src/scene/yaml_scene_loader.cpp` today:
```cpp
CpuCameraPreset parse_camera_preset(const YAML::Node& node) {
    if (const YAML::Node vfov = node["vfov"]) {
        camera.vfov = vfov.as<double>();
    }
    ...
}

RealtimeViewPreset parse_realtime_preset(const YAML::Node& node) {
    if (const YAML::Node vfov_deg = node["vfov_deg"]) {
        preset.vfov_deg = vfov_deg.as<double>();
    }
    if (const YAML::Node use_default_viewer_intrinsics = node["use_default_viewer_intrinsics"]) {
        preset.use_default_viewer_intrinsics = use_default_viewer_intrinsics.as<bool>();
    }
    ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace legacy YAML camera parsing with CameraSpec validation</name>
  <read_first>
src/scene/camera_spec.h
src/scene/yaml_scene_loader.cpp
tests/test_yaml_scene_loader.cpp
.planning/phases/01-shared-camera-schema/01-CONTEXT.md
  </read_first>
  <files>src/scene/yaml_scene_loader.cpp, tests/test_yaml_scene_loader.cpp</files>
  <action>Replace the legacy `parse_camera_preset(...)` and realtime camera parsing path with `CameraSpec`-based helpers per D-02, D-04, D-05, and D-09. Require `camera.model`, `width`, `height`, `fx`, `fy`, `cx`, `cy`, and a `T_bc` map; parse `pinhole32` and `equi62_lut1d` parameter blocks into zero-initialized model slots; and reject missing model, unsupported model strings, malformed `camera`/`T_bc` maps, or legacy-only `vfov`, `vfov_deg`, and `use_default_viewer_intrinsics` camera authoring. Keep `initial_body_pose`, `frame_convention`, and `base_move_speed` as outer realtime preset metadata. Update `tests/test_yaml_scene_loader.cpp` fixture YAML and add explicit failure coverage for missing `model` and legacy field usage.</action>
  <verify>
    <automated>cmake --build build --target test_yaml_scene_loader -j4 && ctest --test-dir build -R '^test_yaml_scene_loader$' --output-on-failure</automated>
  </verify>
  <acceptance_criteria>`ctest --test-dir build -R '^test_yaml_scene_loader$' --output-on-failure` passes, and `rg -n "model|width|height|fx|fy|cx|cy|T_bc" src/scene/yaml_scene_loader.cpp tests/test_yaml_scene_loader.cpp` shows CameraSpec parsing plus negative coverage for missing `model`.</acceptance_criteria>
  <done>The YAML loader only accepts the canonical authored camera schema for project-owned content, and its regression fixtures cover both successful CameraSpec parsing and explicit rejection of legacy-only declarations.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate repo-owned YAML scenes and prove builtin/file-backed equivalence</name>
  <read_first>
assets/scenes/cornell_box/scene.yaml
assets/scenes/final_room/scene.yaml
assets/scenes/imported_obj_smoke/scene.yaml
assets/scenes/simple_light/scene.yaml
tests/test_shared_scene_regression.cpp
  </read_first>
  <files>assets/scenes/cornell_box/scene.yaml, assets/scenes/final_room/scene.yaml, assets/scenes/imported_obj_smoke/scene.yaml, assets/scenes/simple_light/scene.yaml, tests/test_shared_scene_regression.cpp</files>
  <action>Migrate every repo-owned YAML camera declaration per D-04 and D-08 so each `cpu_presets.*.camera` and `realtime.default_view.camera` block explicitly sets `model`, `width`, `height`, `fx`, `fy`, `cx`, `cy`, identity `T_bc`, and a zero-valued model-specific parameter block. Encode the same pinhole intrinsics currently implied by each old YAML `vfov` or default-viewer flag so structural behavior stays stable, and do not add fisheye defaults or Phase 4 utility logic. Update `tests/test_shared_scene_regression.cpp` to assert YAML-loaded and catalog-overlaid scenes preserve `camera.model`, `camera.fx`, `camera.fy`, `camera.cx`, and `camera.cy` for representative scenes, while existing scene dependency and render smoke checks remain intact.</action>
  <verify>
    <automated>cmake --build build --target test_shared_scene_regression test_yaml_scene_loader test_scene_file_catalog -j4 && ctest --test-dir build -R '^(test_shared_scene_regression|test_yaml_scene_loader|test_scene_file_catalog)$' --output-on-failure</automated>
  </verify>
  <acceptance_criteria>`rg -n "model:|fx:|fy:|cx:|cy:|T_bc:" assets/scenes/cornell_box/scene.yaml assets/scenes/final_room/scene.yaml assets/scenes/imported_obj_smoke/scene.yaml assets/scenes/simple_light/scene.yaml` finds explicit camera schema in every repo-owned scene file, and the shared-scene regression slice passes.</acceptance_criteria>
  <done>All repo-owned YAML scenes use the canonical explicit camera schema, and builtin vs file-backed catalog paths preserve the same camera model/intrinsics data under structural regression coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML scene files -> YAML loader | Authored file input crosses into trusted scene-definition data structures |
| YAML definitions -> scene catalog overlay | File-backed scenes replace builtin records and must preserve intended camera semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | T | `src/scene/yaml_scene_loader.cpp` | mitigate | Reject missing `model`, malformed `camera`/`T_bc` maps, unsupported model strings, and legacy-only camera fields so file-backed scenes cannot silently fall back to ambiguous defaults |
| T-01-03-02 | D | `assets/scenes/*.yaml` | mitigate | Migrate every repo-owned YAML camera block to explicit `width/height/fx/fy/cx/cy` plus zero-distortion slots so later default changes do not alter current semantics |
| T-01-03-03 | R | `tests/test_shared_scene_regression.cpp` | mitigate | Assert builtin/file-backed equivalence on camera model and intrinsics so catalog overlay regressions are detected immediately |
</threat_model>

<verification>
Build and run the YAML loader and shared-scene regression slice, then grep every repo-owned `scene.yaml` for explicit camera schema fields.
</verification>

<success_criteria>
Repo-owned YAML scenes declare cameras with explicit `model` and shared intrinsics, the loader enforces that schema, and catalog regression tests prove builtin and file-backed scene paths preserve the same camera data for downstream renderers.
</success_criteria>

<output>
After completion, create `.planning/phases/01-shared-camera-schema/01-03-SUMMARY.md`
</output>
