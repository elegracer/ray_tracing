---
phase: 05-camera-model-regression-coverage
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - tests/test_offline_shared_scene_renderer.cpp
  - tests/test_optix_direction.cpp
  - tests/test_reference_vs_realtime.cpp
autonomous: true
requirements:
  - VER-03
must_haves:
  truths:
    - "Offline CPU and realtime GPU prove the same world-space primary rays for the same packed camera and pixel centers."
    - "Cross-path smoke coverage includes both an explicit pinhole path and a helper-derived default fisheye path."
    - "Image-level checks remain secondary smoke assertions rather than the main mathematical contract."
  artifacts:
    - path: "tests/test_offline_shared_scene_renderer.cpp"
      provides: "Offline-side ray contract anchored to canonical packed-camera fixtures"
    - path: "tests/test_optix_direction.cpp"
      provides: "Realtime GPU direction-debug parity using the same packed-camera fixtures"
    - path: "tests/test_reference_vs_realtime.cpp"
      provides: "Cross-path smoke coverage for explicit pinhole and helper-derived fisheye defaults"
  key_links:
    - from: "tests/test_offline_shared_scene_renderer.cpp"
      to: "tests/test_optix_direction.cpp"
      via: "shared `PackedCamera` fixtures and pixel-center ray agreement"
      pattern: "debug_primary_ray|render_direction_debug|PackedCamera"
    - from: "src/realtime/camera_models.cpp"
      to: "tests/test_reference_vs_realtime.cpp"
      via: "default-intrinsics helper drives the helper-derived fisheye smoke path"
      pattern: "derive_default_camera_intrinsics|default_hfov_deg|equi62_lut1d"
---

<objective>
Prove the offline CPU and realtime GPU paths honor the same selected camera model and derived defaults.

Purpose: satisfy VER-03 with direction-level contract proof first and image-level smoke second.
Output: aligned offline and GPU regression tests that compare the same packed-camera fixtures and a smoke test that exercises explicit pinhole plus helper-derived default fisheye.
</objective>

<execution_context>
@/home/huangkai/.codex/get-shit-done/workflows/execute-plan.md
@/home/huangkai/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-camera-model-regression-coverage/05-CONTEXT.md
@.planning/phases/05-camera-model-regression-coverage/05-RESEARCH.md
@.planning/phases/05-camera-model-regression-coverage/05-PATTERNS.md
@src/common/camera.h
@src/core/offline_shared_scene_renderer.cpp
@src/realtime/camera_models.cpp
@src/realtime/gpu/programs.cu
@tests/test_offline_shared_scene_renderer.cpp
@tests/test_optix_direction.cpp
@tests/test_reference_vs_realtime.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align offline and GPU direction-contract checks around the same packed-camera fixtures</name>
  <read_first>
tests/test_offline_shared_scene_renderer.cpp
tests/test_optix_direction.cpp
src/common/camera.h
  </read_first>
  <files>tests/test_offline_shared_scene_renderer.cpp, tests/test_optix_direction.cpp</files>
  <action>Refactor or extend the existing test fixtures so the offline seam and the OptiX direction-debug path both validate the same representative packed cameras and pixel-center samples. Keep the comparison at the world-space ray level: offline should still go through `Camera::debug_primary_ray(...)`, realtime should still go through direction-debug output, and the resulting directions should agree for both explicit pinhole and explicit equi fixtures. Avoid adding new production debug APIs unless the existing seams prove insufficient.</action>
  <verify>
    <automated>cmake --build build --target test_offline_shared_scene_renderer test_optix_direction -j4 && ctest --test-dir build -R '^(test_offline_shared_scene_renderer|test_optix_direction)$' --output-on-failure</automated>
  </verify>
  <acceptance_criteria>The same packed-camera fixtures are visibly shared across the offline and GPU tests, both supported models are covered, and direction comparisons are expressed in world space at pixel centers rather than through weaker image-level proxies.</acceptance_criteria>
  <done>The project has a direct offline-vs-GPU ray-contract proof using the same camera inputs.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade cross-path smoke coverage to include explicit pinhole and helper-derived fisheye defaults</name>
  <read_first>
tests/test_reference_vs_realtime.cpp
src/realtime/camera_models.cpp
tests/test_realtime_scene_factory.cpp
  </read_first>
  <files>tests/test_reference_vs_realtime.cpp</files>
  <action>Upgrade `tests/test_reference_vs_realtime.cpp` so it no longer treats one legacy pinhole `Camera` setup as the only cross-path check. Keep the test lightweight, but add at least two smoke cases: one explicit pinhole path and one helper-derived default fisheye path built from the shared default-intrinsics helper. Use coarse image-level or luminance assertions only as smoke confirmation that the stronger ray-level contract still produces sane renders, and keep the main mathematical truth in Task 1.</action>
  <verify>
    <automated>cmake --build build --target test_reference_vs_realtime test_offline_shared_scene_renderer test_optix_direction -j4 && ctest --test-dir build -R '^(test_reference_vs_realtime|test_offline_shared_scene_renderer|test_optix_direction)$' --output-on-failure</automated>
  </verify>
  <acceptance_criteria>`test_reference_vs_realtime` clearly covers explicit pinhole and helper-derived fisheye defaults, does not become the sole source of mathematical truth, and remains fast enough to be part of the Phase 5 slice.</acceptance_criteria>
  <done>Cross-path smoke coverage now reflects the project’s real camera-model contract instead of only the old pinhole path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| offline packed-camera seam -> GPU active-camera seam | The same camera payload must produce the same world-space primary rays at the same pixel centers |
| ray-level contract -> image-level smoke | Smoke coverage must confirm sanity without replacing the stronger direction-level proof |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | T | offline/GPU fixture drift | mitigate | Reuse the same packed-camera fixtures and pixel samples across `test_offline_shared_scene_renderer` and `test_optix_direction` |
| T-05-02-02 | R | `test_reference_vs_realtime.cpp` | mitigate | Keep image-level assertions coarse and explicitly secondary to the ray-level contract |
| T-05-02-03 | D | helper-derived fisheye defaults | mitigate | Add a smoke case that uses the shared default-intrinsics helper rather than hard-coded pinhole-only setup |
</threat_model>

<verification>
Run the offline seam, OptiX direction, and cross-path smoke tests together, then inspect the assertions to confirm the same camera fixtures drive both the CPU and GPU comparisons.
</verification>

<success_criteria>
The repo proves that offline CPU and realtime GPU honor the same per-camera model and default intrinsics at the ray-contract level, with explicit pinhole and helper-derived fisheye smoke coverage on top.
</success_criteria>

<output>
After completion, create `.planning/phases/05-camera-model-regression-coverage/05-02-SUMMARY.md`
</output>
