---
phase: 05-camera-model-regression-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CMakeLists.txt
  - tests/test_camera_models.cpp
autonomous: true
requirements:
  - VER-01
  - VER-02
must_haves:
  truths:
    - "Representative `pinhole32` samples are validated directly against `docs/reference/src-cam/cam_pinhole32.h`."
    - "Representative `equi62_lut1d` samples are validated directly against `docs/reference/src-cam/cam_equi62_lut1d.h`."
    - "Model-appropriate tolerances are explicit, with tighter bounds for pinhole and slightly looser bounds for LUT-backed equi."
  artifacts:
    - path: "tests/test_camera_models.cpp"
      provides: "Reference-backed parity checks for both supported camera models"
    - path: "CMakeLists.txt"
      provides: "Any minimal include-path or build plumbing needed for test-only reference-header access"
  key_links:
    - from: "docs/reference/src-cam/cam_pinhole32.h"
      to: "tests/test_camera_models.cpp"
      via: "direct representative parity samples"
      pattern: "CamPinhole32|project|unproject"
    - from: "docs/reference/src-cam/cam_equi62_lut1d.h"
      to: "tests/test_camera_models.cpp"
      via: "direct representative parity samples"
      pattern: "CamEqui62Lut1D|project|unproject"
---

<objective>
Anchor the repo camera-model math directly to the bundled reference headers.

Purpose: satisfy VER-01 and VER-02 with minimal, test-focused changes.
Output: `test_camera_models` directly proves representative `pinhole32` and `equi62_lut1d` projection/unprojection parity against the reference headers, using explicit tolerances and no duplicated reference formulas.
</objective>

<execution_context>
@/home/huangkai/.codex/get-shit-done/workflows/execute-plan.md
@/home/huangkai/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-camera-model-regression-coverage/05-CONTEXT.md
@.planning/phases/05-camera-model-regression-coverage/05-RESEARCH.md
@.planning/phases/05-camera-model-regression-coverage/05-PATTERNS.md
@CMakeLists.txt
@docs/reference/src-cam/cam_pinhole32.h
@docs/reference/src-cam/cam_equi62_lut1d.h
@tests/test_camera_models.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make `test_camera_models` consume the bundled reference implementations without cloning their math</name>
  <read_first>
CMakeLists.txt
docs/reference/src-cam/cam_pinhole32.h
docs/reference/src-cam/cam_equi62_lut1d.h
tests/test_camera_models.cpp
  </read_first>
  <files>CMakeLists.txt, tests/test_camera_models.cpp</files>
  <action>Add the smallest possible test-only integration so `tests/test_camera_models.cpp` can call the reference camera implementations in `docs/reference/src-cam/`. If the target needs extra include directories or a small local adapter for parameter conversion, keep that plumbing inside the test target or test file. Do not copy the reference formulas into repo production code or into a second standalone helper library.</action>
  <verify>
    <automated>cmake --build build --target test_camera_models -j4 && ctest --test-dir build -R '^test_camera_models$' --output-on-failure</automated>
  </verify>
  <acceptance_criteria>`test_camera_models` builds cleanly while directly reaching the reference headers, and the diff shows no new production camera logic or duplicate reference math outside the test path.</acceptance_criteria>
  <done>The repo can compile a test that compares its camera math against the bundled reference implementations.</done>
</task>

<task type="auto">
  <name>Task 2: Add representative pinhole and equi parity assertions with explicit tolerances</name>
  <read_first>
tests/test_camera_models.cpp
docs/reference/src-cam/cam_pinhole32.h
docs/reference/src-cam/cam_equi62_lut1d.h
  </read_first>
  <files>tests/test_camera_models.cpp</files>
  <action>Extend `tests/test_camera_models.cpp` with representative projection and unprojection samples for both supported models, using stable parameter fixtures that cover at least center, off-axis, and distorted or LUT-backed cases. Compare the repo `project_*` and `unproject_*` results directly against the reference-header outputs with explicit per-model tolerances. Include a helper-derived default fisheye intrinsics case in the equi coverage so Phase 4’s default path is part of the parity proof.</action>
  <verify>
    <automated>cmake --build build --target test_camera_models -j4 && ctest --test-dir build -R '^test_camera_models$' --output-on-failure</automated>
  </verify>
  <acceptance_criteria>`test_camera_models` now contains direct reference-backed parity checks for both models, pinhole tolerances stay tight, equi tolerances are justified by LUT behavior, and existing roundtrip/default-intrinsics checks remain green.</acceptance_criteria>
  <done>The low-level camera-model regression target proves representative repo math matches the bundled reference implementations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bundled reference headers -> repo test assertions | The parity proof must call the actual bundled reference implementations rather than an independently re-derived approximation |
| repo camera math -> tolerance policy | Tolerances must be strict enough to detect drift but realistic for LUT-backed equi interpolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | T | `tests/test_camera_models.cpp` | mitigate | Directly call the reference headers instead of duplicating their formulas in custom test helpers |
| T-05-01-02 | I | parity tolerances | mitigate | Use separate explicit tolerances for pinhole and equi and document why they differ |
| T-05-01-03 | R | `CMakeLists.txt` test plumbing | mitigate | Keep any include-path or adapter changes test-local and verify `test_camera_models` remains the only affected target |
</threat_model>

<verification>
Build and run `test_camera_models`, then inspect the diff to confirm reference parity lives in the existing math test and did not create new production camera code.
</verification>

<success_criteria>
The repo has direct, representative, reference-backed parity proof for both `pinhole32` and `equi62_lut1d` inside the existing low-level camera-model regression target.
</success_criteria>

<output>
After completion, create `.planning/phases/05-camera-model-regression-coverage/05-01-SUMMARY.md`
</output>
