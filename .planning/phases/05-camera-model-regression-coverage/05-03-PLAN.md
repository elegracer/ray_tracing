---
phase: 05-camera-model-regression-coverage
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - tests/test_realtime_scene_factory.cpp
  - tests/test_shared_scene_regression.cpp
  - tests/test_viewer_scene_reload.cpp
autonomous: true
requirements:
  - VER-04
must_haves:
  truths:
    - "Helper-generated defaults still resolve to `equi62_lut1d` after the stronger parity work lands."
    - "Explicitly authored pinhole scenes and presets still remain pinhole after the default switch."
    - "The widened Phase 5 slice stays green, including live OptiX fisheye and viewer-quality regressions."
  artifacts:
    - path: "tests/test_realtime_scene_factory.cpp"
      provides: "Default-helper versus authored-preset camera-model regressions"
    - path: "tests/test_shared_scene_regression.cpp"
      provides: "Shared-scene and file-backed authored/default camera preservation coverage"
    - path: "tests/test_viewer_scene_reload.cpp"
      provides: "File-backed equi reload/rescan coverage under the widened regression slice"
  key_links:
    - from: "tests/test_realtime_scene_factory.cpp"
      to: "tests/test_shared_scene_regression.cpp"
      via: "implicit fisheye defaults versus explicit pinhole authorship"
      pattern: "earth_sphere|final_room|pinhole32|equi62_lut1d"
    - from: "tests/test_viewer_scene_reload.cpp"
      to: "tests/test_optix_equi_path_trace.cpp"
      via: "file-backed equi scenes remain live through reload and OptiX rendering"
      pattern: "reload|rescan|equi62_lut1d|render_radiance"
---

<objective>
Lock the milestone with a widened authored-vs-default regression slice.

Purpose: satisfy VER-04 by tightening the existing regression lattice rather than adding new production behavior.
Output: sharper default-vs-authored assertions in scene and reload tests, plus a final widened Phase 5 validation slice that remains green end-to-end.
</objective>

<execution_context>
@/home/huangkai/.codex/get-shit-done/workflows/execute-plan.md
@/home/huangkai/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-camera-model-regression-coverage/05-CONTEXT.md
@.planning/phases/05-camera-model-regression-coverage/05-RESEARCH.md
@.planning/phases/05-camera-model-regression-coverage/05-PATTERNS.md
@tests/test_realtime_scene_factory.cpp
@tests/test_shared_scene_regression.cpp
@tests/test_viewer_scene_reload.cpp
@tests/test_viewer_quality_reference.cpp
@tests/test_optix_equi_path_trace.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten authored-versus-default camera assertions across scene-factory and shared-scene regressions</name>
  <read_first>
tests/test_realtime_scene_factory.cpp
tests/test_shared_scene_regression.cpp
.planning/phases/05-camera-model-regression-coverage/05-CONTEXT.md
  </read_first>
  <files>tests/test_realtime_scene_factory.cpp, tests/test_shared_scene_regression.cpp</files>
  <action>Strengthen the existing regression assertions so the Phase 5 coverage matrix is explicit in code, not only implied by earlier phases. Ensure the tests clearly assert that helper-generated defaults such as `earth_sphere` stay `equi62_lut1d`, explicitly authored presets such as `final_room` remain `pinhole32`, and file-backed/shared-scene paths preserve the same authored/default distinction after the default switch.</action>
  <verify>
    <automated>cmake --build build --target test_realtime_scene_factory test_shared_scene_regression -j4 && ctest --test-dir build -R '^(test_realtime_scene_factory|test_shared_scene_regression)$' --output-on-failure</automated>
  </verify>
  <acceptance_criteria>The authored/default split is expressed with direct assertions in both tests, covering repo-owned helper-generated defaults, explicit authored pinhole presets, and at least one file-backed/shared-scene path.</acceptance_criteria>
  <done>The widened regression lattice now states the default-versus-authored camera contract explicitly.</done>
</task>

<task type="auto">
  <name>Task 2: Keep file-backed equi reload and the widened final slice green</name>
  <read_first>
tests/test_viewer_scene_reload.cpp
tests/test_viewer_quality_reference.cpp
tests/test_optix_equi_path_trace.cpp
  </read_first>
  <files>tests/test_viewer_scene_reload.cpp</files>
  <action>If needed, strengthen `tests/test_viewer_scene_reload.cpp` so file-backed equi reload and rescan behavior is explicitly part of the Phase 5 default-switch closure. Then run the widened Phase 5 slice, including `test_viewer_quality_reference` and `test_optix_equi_path_trace`, and only adjust nearby test assertions if the stronger parity work made an implicit contract ambiguous. Do not expand production scope here; this task is about final regression closure.</action>
  <verify>
    <automated>cmake --build build --target test_camera_models test_offline_shared_scene_renderer test_optix_direction test_reference_vs_realtime test_viewer_quality_reference test_realtime_scene_factory test_shared_scene_regression test_viewer_scene_reload test_optix_equi_path_trace -j4 && ctest --test-dir build -R '^(test_camera_models|test_offline_shared_scene_renderer|test_optix_direction|test_reference_vs_realtime|test_viewer_quality_reference|test_realtime_scene_factory|test_shared_scene_regression|test_viewer_scene_reload|test_optix_equi_path_trace)$' --output-on-failure</automated>
  </verify>
  <acceptance_criteria>The full widened Phase 5 slice passes, and the final regressions still demonstrate live OptiX equi rendering plus file-backed reload stability after the stronger parity assertions land.</acceptance_criteria>
  <done>The milestone has a widened, explicit, and fully green regression lock after the fisheye-default rollout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| helper-generated defaults -> explicit authored presets | Default fisheye behavior must not silently rewrite explicit pinhole authorship |
| file-backed reload/rescan -> live viewer and OptiX paths | Reloaded equi scenes must remain live and correctly typed through runtime refresh and rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | T | default-vs-authored regressions | mitigate | Add explicit assertions in `test_realtime_scene_factory` and `test_shared_scene_regression` instead of relying on indirect behavior |
| T-05-03-02 | R | file-backed reload coverage | mitigate | Keep `test_viewer_scene_reload` in the widened final slice and sharpen its model-preservation assertions if needed |
| T-05-03-03 | D | widened final slice | mitigate | Run the full mandatory slice including `test_viewer_quality_reference` and `test_optix_equi_path_trace` before Phase 5 verification |
</threat_model>

<verification>
Run the full widened Phase 5 slice and confirm it still includes scene-factory, shared-scene, viewer reload, viewer quality, and live OptiX fisheye coverage.
</verification>

<success_criteria>
The Phase 5 regression lattice explicitly proves helper-generated fisheye defaults coexist with explicit pinhole authorship, and the whole widened slice stays green.
</success_criteria>

<output>
After completion, create `.planning/phases/05-camera-model-regression-coverage/05-03-SUMMARY.md`
</output>
