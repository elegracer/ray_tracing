---
phase: 03-realtime-gpu-and-viewer-camera-models
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/realtime/viewer/four_camera_rig.h
  - src/realtime/viewer/four_camera_rig.cpp
  - src/realtime/realtime_scene_factory.cpp
  - tests/test_realtime_scene_factory.cpp
  - tests/test_viewer_four_camera_rig.cpp
autonomous: true
requirements:
  - CAM-04
must_haves:
  truths:
    - "Viewer default rig construction derives model and intrinsics from authored `CameraSpec` data instead of a hardcoded pinhole block."
    - "Viewer yaw-offset, frame-convention, and body-pose behavior stay intact while the camera payload becomes spec-driven."
    - "Spec-driven four-camera viewer rigs can preserve per-camera model choice instead of forcing one pinhole model."
  artifacts:
    - path: "src/realtime/viewer/four_camera_rig.cpp"
      provides: "Spec-driven viewer rig builder preserving existing pose/yaw semantics"
    - path: "src/realtime/realtime_scene_factory.cpp"
      provides: "Scene-preset-driven viewer/runtime camera alignment"
    - path: "tests/test_viewer_four_camera_rig.cpp"
      provides: "Viewer rig contract regressions for authored and mixed-model specs"
  key_links:
    - from: "src/realtime/realtime_scene_factory.cpp"
      to: "src/realtime/viewer/four_camera_rig.cpp"
      via: "shared runtime camera-spec scaling and pose semantics"
      pattern: "runtime_camera_spec|CameraSpec|kDefaultSurroundYawOffsetsDeg"
    - from: "src/realtime/viewer/four_camera_rig.cpp"
      to: "src/realtime/camera_rig.cpp"
      via: "canonical `CameraSpec` -> `CameraRig::add_camera(...)` path"
      pattern: "add_camera\\("
---

<objective>
Replace the viewer's hardcoded pinhole rig with a spec-driven runtime rig builder.

Purpose: satisfy D-01 through D-04 and CAM-04 by making viewer default rigs preserve authored camera model/intrinsics while keeping the current pose and yaw-offset semantics intact.
Output: viewer rig construction that consumes `CameraSpec` inputs, plus regressions that prove authored equi and mixed-model camera specs stay intact through the viewer/runtime path.
</objective>

<execution_context>
@/home/huangkai/.codex/get-shit-done/workflows/execute-plan.md
@/home/huangkai/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-realtime-gpu-and-viewer-camera-models/03-CONTEXT.md
@.planning/phases/03-realtime-gpu-and-viewer-camera-models/03-RESEARCH.md
@.planning/phases/03-realtime-gpu-and-viewer-camera-models/03-PATTERNS.md
@src/realtime/realtime_scene_factory.cpp
@src/realtime/viewer/four_camera_rig.h
@src/realtime/viewer/four_camera_rig.cpp
@src/realtime/camera_rig.h
@src/realtime/camera_rig.cpp
@tests/test_realtime_scene_factory.cpp
@tests/test_viewer_four_camera_rig.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make viewer rig construction spec-driven instead of pinhole-hardcoded</name>
  <read_first>
src/realtime/viewer/four_camera_rig.h
src/realtime/viewer/four_camera_rig.cpp
src/realtime/realtime_scene_factory.cpp
.planning/phases/03-realtime-gpu-and-viewer-camera-models/03-CONTEXT.md
  </read_first>
  <files>src/realtime/viewer/four_camera_rig.h, src/realtime/viewer/four_camera_rig.cpp, src/realtime/realtime_scene_factory.cpp</files>
  <action>Refactor `make_default_viewer_rig(...)` so it no longer constructs a local `Pinhole32Params pinhole { ... }` block. Introduce a spec-driven helper that accepts canonical camera input as `scene::CameraSpec` (either one authored spec cloned across the four yaw offsets or a fixed-size per-camera collection for mixed rigs), then applies the existing viewer pose/yaw/frame-convention math and calls `CameraRig::add_camera(...)` for each slot. Preserve `kDefaultSurroundYawOffsetsDeg`, translation from `BodyPose`, and the current `forward_direction(...)` / `right_direction(...)` behavior exactly. Reuse the same runtime intrinsics scaling rule as `runtime_camera_spec(...)`: `fx`, `fy`, `cx`, and `cy` must scale from authored width/height to requested runtime width/height instead of inventing a separate viewer formula.</action>
  <verify>
    <automated>cmake --build build --target test_realtime_scene_factory test_viewer_four_camera_rig -j4 && ctest --test-dir build -R '^(test_realtime_scene_factory|test_viewer_four_camera_rig)$' --output-on-failure</automated>
  </verify>
  <acceptance_criteria>`test_realtime_scene_factory` and `test_viewer_four_camera_rig` both pass, `rg -n "Pinhole32Params pinhole" src/realtime/viewer/four_camera_rig.cpp` returns no hardcoded viewer-camera block, and `rg -n "add_camera\\(" src/realtime/viewer/four_camera_rig.cpp` shows viewer rigs are now created from canonical camera specs.</acceptance_criteria>
  <done>The viewer helper now derives runtime cameras from `CameraSpec` inputs while preserving existing pose/yaw semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the authored-spec and mixed-model viewer rig contract in tests</name>
  <read_first>
tests/test_realtime_scene_factory.cpp
tests/test_viewer_four_camera_rig.cpp
src/realtime/viewer/four_camera_rig.h
.planning/phases/03-realtime-gpu-and-viewer-camera-models/03-CONTEXT.md
  </read_first>
  <files>tests/test_realtime_scene_factory.cpp, tests/test_viewer_four_camera_rig.cpp</files>
  <action>Extend the realtime/viewer tests so they explicitly cover the new contract. In `tests/test_realtime_scene_factory.cpp`, keep the existing scene-preset alignment checks and add authored-camera assertions that a runtime equi camera spec preserves `model`, scaled intrinsics, and pose. In `tests/test_viewer_four_camera_rig.cpp`, add a mixed-model spec-driven four-camera case such as `[front=pinhole32, left=equi62_lut1d, right=pinhole32, rear=equi62_lut1d]` and assert each packed camera keeps its selected `model`, requested width/height, and the current yaw-offset/body-pose orientation semantics. Do not weaken the existing movement or pose assertions.</action>
  <verify>
    <automated>cmake --build build --target test_realtime_scene_factory test_viewer_four_camera_rig -j4 && ctest --test-dir build -R '^(test_realtime_scene_factory|test_viewer_four_camera_rig)$' --output-on-failure</automated>
  </verify>
  <acceptance_criteria>The updated tests explicitly assert authored equi spec preservation and mixed-model four-camera viewer rig packing, and the same two test binaries pass without removing any existing pose/movement expectations.</acceptance_criteria>
  <done>Phase 3 has direct regression proof that viewer rigs preserve authored camera payloads and can represent mixed-model four-camera layouts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored camera spec -> viewer runtime rig | Viewer code must not reinterpret or replace authored model/intrinsics with a hardcoded pinhole convention |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | T | `src/realtime/viewer/four_camera_rig.cpp` | mitigate | Remove the local hardcoded pinhole camera block and route viewer rig creation through canonical `CameraSpec` + `CameraRig::add_camera(...)` |
| T-03-01-02 | R | viewer pose/yaw logic | mitigate | Preserve existing yaw-offset/body-pose math and assert it explicitly in `test_viewer_four_camera_rig` |
| T-03-01-03 | D | mixed-model viewer rigs | mitigate | Add direct mixed-model four-camera regressions so per-camera model preservation is enforced before GPU work begins |
</threat_model>

<verification>
Build and run `test_realtime_scene_factory` and `test_viewer_four_camera_rig`, then grep `four_camera_rig.cpp` to confirm viewer camera payloads are no longer hardcoded pinhole params.
</verification>

<success_criteria>
Viewer default rig creation preserves authored camera model and intrinsics through a spec-driven path, while keeping the existing viewer pose/yaw semantics and enabling mixed-model four-camera rigs.
</success_criteria>

<output>
After completion, create `.planning/phases/03-realtime-gpu-and-viewer-camera-models/03-01-SUMMARY.md`
</output>
