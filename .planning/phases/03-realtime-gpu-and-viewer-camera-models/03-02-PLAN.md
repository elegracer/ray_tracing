---
phase: 03-realtime-gpu-and-viewer-camera-models
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/realtime/gpu/optix_renderer.cpp
  - src/realtime/gpu/programs.cu
  - tests/test_optix_direction.cpp
  - tests/test_optix_equi_path_trace.cpp
autonomous: true
requirements:
  - CAM-03
must_haves:
  truths:
    - "Realtime GPU launches consume `PackedCamera.model` and the correct per-model params instead of silently behaving like pinhole."
    - "An equi active camera can produce a non-black realtime radiance frame through the live OptiX path."
    - "Mixed-model rigs preserve camera selection by `camera_index` without cross-contaminating per-camera payloads."
  artifacts:
    - path: "src/realtime/gpu/optix_renderer.cpp"
      provides: "Model-preserving upload from `PackedCamera` to `DeviceActiveCamera`"
    - path: "src/realtime/gpu/programs.cu"
      provides: "Model-aware device-side ray generation for the active realtime camera"
    - path: "tests/test_optix_equi_path_trace.cpp"
      provides: "Live OptiX equi and mixed-camera regression coverage"
  key_links:
    - from: "src/realtime/camera_rig.h"
      to: "src/realtime/gpu/optix_renderer.cpp"
      via: "`PackedCamera` -> `DeviceActiveCamera` upload"
      pattern: "PackedCamera|DeviceActiveCamera|CameraModelType"
    - from: "src/realtime/gpu/optix_renderer.cpp"
      to: "src/realtime/gpu/programs.cu"
      via: "active-camera launch params"
      pattern: "active_camera|unproject_(pinhole32|equi62_lut1d)"
---

<objective>
Lock the GPU side of the camera-model contract and recover equi realtime rendering.

Purpose: satisfy CAM-03 by ensuring the active realtime camera uses its selected model and intrinsics all the way through OptiX upload and ray generation, then prove the equi path produces live image content.
Output: model-preserving GPU camera upload plus OptiX regressions for equi and mixed-model active-camera rendering.
</objective>

<execution_context>
@/home/huangkai/.codex/get-shit-done/workflows/execute-plan.md
@/home/huangkai/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-realtime-gpu-and-viewer-camera-models/03-CONTEXT.md
@.planning/phases/03-realtime-gpu-and-viewer-camera-models/03-RESEARCH.md
@.planning/phases/03-realtime-gpu-and-viewer-camera-models/03-PATTERNS.md
@.planning/phases/03-realtime-gpu-and-viewer-camera-models/03-01-PLAN.md
@src/realtime/camera_rig.h
@src/realtime/gpu/launch_params.h
@src/realtime/gpu/optix_renderer.cpp
@src/realtime/gpu/programs.cu
@tests/test_optix_direction.cpp
@tests/test_optix_equi_path_trace.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preserve packed camera model data through the OptiX upload path</name>
  <read_first>
src/realtime/gpu/launch_params.h
src/realtime/gpu/optix_renderer.cpp
src/realtime/gpu/programs.cu
tests/test_optix_direction.cpp
.planning/phases/03-realtime-gpu-and-viewer-camera-models/03-RESEARCH.md
  </read_first>
  <files>src/realtime/gpu/optix_renderer.cpp, src/realtime/gpu/programs.cu, tests/test_optix_direction.cpp</files>
  <action>Audit and fix the active-camera contract so `PackedCamera.model`, `PackedCamera.pinhole`, `PackedCamera.equi`, and `PackedCamera.T_rc` survive unchanged into `DeviceActiveCamera` and the device-side ray-generation path. Keep the existing model dispatch shape in `programs.cu`, but ensure the active camera selected for the launch actually drives `unproject_equi62_lut1d(...)` when `model == CameraModelType::equi62_lut1d`. Extend `tests/test_optix_direction.cpp` so it no longer only smoke-tests a one-camera pinhole rig; add an equi camera smoke case and at least one mixed-model rig case where the selected active camera is not the default pinhole front camera.</action>
  <verify>
    <automated>cmake --build build --target test_optix_direction -j4 && ctest --test-dir build -R '^test_optix_direction$' --output-on-failure</automated>
  </verify>
  <acceptance_criteria>`test_optix_direction` passes with explicit equi coverage, and `rg -n "camera.model == CameraModelType::equi62_lut1d|active\\.equi|active\\.pinhole" src/realtime/gpu/programs.cu src/realtime/gpu/optix_renderer.cpp` shows the launch path still keys directly off the packed active camera model and per-model payload.</acceptance_criteria>
  <done>The OptiX launch path preserves camera model choice and can at least smoke-test equi and mixed-model active-camera selection.</done>
</task>

<task type="auto">
  <name>Task 2: Recover and regress live equi radiance rendering</name>
  <read_first>
tests/test_optix_equi_path_trace.cpp
src/realtime/gpu/optix_renderer.cpp
src/realtime/gpu/programs.cu
.planning/phases/03-realtime-gpu-and-viewer-camera-models/03-RESEARCH.md
  </read_first>
  <files>src/realtime/gpu/optix_renderer.cpp, src/realtime/gpu/programs.cu, tests/test_optix_equi_path_trace.cpp</files>
  <action>Fix the failing realtime equi radiance path so `test_optix_equi_path_trace` stops aborting with `equi radiance should be non-black`. Keep the phase scope focused on camera-model preservation: use the existing scene/light setup in the test, and adjust camera upload, basis construction, active-camera selection, or equi ray generation only as needed to make the live OptiX path honor the selected equi camera. Expand the test to cover a mixed-model rig where camera index selects the equi camera slot explicitly, then assert non-black beauty output, non-empty G-buffer channels, and varying depth for that selected equi camera.</action>
  <verify>
    <automated>cmake --build build --target test_optix_direction test_optix_equi_path_trace -j4 && ctest --test-dir build -R '^(test_optix_direction|test_optix_equi_path_trace)$' --output-on-failure</automated>
  </verify>
  <acceptance_criteria>`test_optix_equi_path_trace` passes, including a selected equi camera in a mixed-model rig, and the test explicitly checks non-black radiance plus non-empty beauty/normal/albedo/depth outputs for that equi render.</acceptance_criteria>
  <done>Realtime GPU rendering now honors equi and mixed-model camera selection through the live OptiX execution path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| packed runtime camera -> OptiX active camera | Model-specific intrinsics and pose must not be lost or coerced during device upload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | T | `src/realtime/gpu/optix_renderer.cpp` | mitigate | Keep `PackedCamera` -> `DeviceActiveCamera` upload field-for-field and regress it with explicit equi smoke coverage |
| T-03-02-02 | R | `src/realtime/gpu/programs.cu` | mitigate | Test model dispatch on the live launch path rather than assuming kernel support from code inspection alone |
| T-03-02-03 | D | `tests/test_optix_equi_path_trace.cpp` | mitigate | Turn the currently failing equi radiance regression into a stable selected-camera contract test before Phase 3 verification |
</threat_model>

<verification>
Build and run the OptiX direction + equi path trace slice, then inspect `optix_renderer.cpp` and `programs.cu` for direct model-aware active-camera upload/dispatch.
</verification>

<success_criteria>
Realtime GPU rendering preserves the selected camera model from packed rig to device launch, and an equi active camera produces valid radiance output through the live OptiX path.
</success_criteria>

<output>
After completion, create `.planning/phases/03-realtime-gpu-and-viewer-camera-models/03-02-SUMMARY.md`
</output>
