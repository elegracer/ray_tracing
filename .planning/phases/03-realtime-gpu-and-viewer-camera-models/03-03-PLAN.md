---
phase: 03-realtime-gpu-and-viewer-camera-models
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - tests/test_realtime_scene_factory.cpp
  - tests/test_viewer_four_camera_rig.cpp
  - tests/test_viewer_scene_reload.cpp
autonomous: true
requirements:
  - CAM-03
  - CAM-04
must_haves:
  truths:
    - "File-backed realtime presets preserve authored camera model and intrinsics through runtime rig creation and viewer reload."
    - "Four-camera mixed-model regressions stay coherent across scene factory, viewer rig, and reload flows."
    - "Phase 3 ends with one consistent realtime/viewer camera contract rather than isolated unit fixes."
  artifacts:
    - path: "tests/test_realtime_scene_factory.cpp"
      provides: "Factory/runtime preservation of authored realtime camera payloads"
    - path: "tests/test_viewer_scene_reload.cpp"
      provides: "File-backed viewer reload coverage for authored realtime camera models"
    - path: "tests/test_viewer_four_camera_rig.cpp"
      provides: "End-to-end mixed-model viewer rig contract coverage"
  key_links:
    - from: "assets/scenes/*/scene.yaml"
      to: "tests/test_viewer_scene_reload.cpp"
      via: "file-backed realtime preset schema and reload path"
      pattern: "model|width|height|fx|fy|cx|cy|T_bc"
    - from: "src/realtime/realtime_scene_factory.cpp"
      to: "tests/test_realtime_scene_factory.cpp"
      via: "authored preset -> runtime rig preservation"
      pattern: "default_camera_rig_for_scene|viewer_frame_convention_for_scene"
---

<objective>
Close Phase 3 with end-to-end realtime/viewer regressions.

Purpose: satisfy D-07, D-08, CAM-03, and CAM-04 by proving file-backed presets, viewer reload, and four-camera mixed-model rigs all preserve the same realtime camera contract after Plans 01 and 02 land.
Output: regression coverage that protects authored realtime presets, viewer reload behavior, and mixed-model four-camera rigs across the full Phase 3 chain.
</objective>

<execution_context>
@/home/huangkai/.codex/get-shit-done/workflows/execute-plan.md
@/home/huangkai/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-realtime-gpu-and-viewer-camera-models/03-CONTEXT.md
@.planning/phases/03-realtime-gpu-and-viewer-camera-models/03-RESEARCH.md
@.planning/phases/03-realtime-gpu-and-viewer-camera-models/03-PATTERNS.md
@.planning/phases/03-realtime-gpu-and-viewer-camera-models/03-02-PLAN.md
@src/realtime/realtime_scene_factory.cpp
@src/realtime/viewer/four_camera_rig.cpp
@tests/test_realtime_scene_factory.cpp
@tests/test_viewer_four_camera_rig.cpp
@tests/test_viewer_scene_reload.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add file-backed realtime preset and reload coverage for authored camera models</name>
  <read_first>
tests/test_realtime_scene_factory.cpp
tests/test_viewer_scene_reload.cpp
src/scene/yaml_scene_loader.cpp
src/realtime/realtime_scene_factory.cpp
.planning/phases/03-realtime-gpu-and-viewer-camera-models/03-CONTEXT.md
  </read_first>
  <files>tests/test_realtime_scene_factory.cpp, tests/test_viewer_scene_reload.cpp</files>
  <action>Create a temp file-backed scene fixture with an explicit realtime `camera:` block authored as `equi62_lut1d` and route it through the existing scene catalog / reload path. In `tests/test_realtime_scene_factory.cpp`, assert the runtime rig preserves the authored `model`, scaled intrinsics, and pose for that file-backed realtime preset. In `tests/test_viewer_scene_reload.cpp`, reload or rescan that temp scene and assert the viewer-side scene switch path preserves the authored camera model instead of collapsing it back to pinhole. Reuse the existing temp-root `scene.yaml` test pattern already used by reload tests; do not modify repo-owned scene assets.</action>
  <verify>
    <automated>cmake --build build --target test_realtime_scene_factory test_viewer_scene_reload -j4 && ctest --test-dir build -R '^(test_realtime_scene_factory|test_viewer_scene_reload)$' --output-on-failure</automated>
  </verify>
  <acceptance_criteria>The realtime factory and viewer reload tests both pass with explicit file-backed equi preset coverage, and the temp YAML fixture asserts `model: equi62_lut1d` survives runtime rig creation and viewer reload/rescan.</acceptance_criteria>
  <done>Phase 3 has file-backed proof that authored realtime camera presets survive the viewer/runtime chain intact.</done>
</task>

<task type="auto">
  <name>Task 2: Close with a coherent mixed-model regression slice</name>
  <read_first>
tests/test_realtime_scene_factory.cpp
tests/test_viewer_four_camera_rig.cpp
tests/test_viewer_scene_reload.cpp
.planning/phases/03-realtime-gpu-and-viewer-camera-models/03-VALIDATION.md
  </read_first>
  <files>tests/test_realtime_scene_factory.cpp, tests/test_viewer_four_camera_rig.cpp, tests/test_viewer_scene_reload.cpp</files>
  <action>Finish Phase 3 by aligning the scene-factory, viewer-rig, and viewer-reload regression slice around one consistent camera contract. Keep the tests explicit: assert per-camera `model`, width/height, key intrinsics (`fx`, `fy`, `cx`, `cy`), and pose/orientation preservation where relevant. Do not add broad image-comparison goldens. The goal is to make future failures clearly attributable to camera-contract drift, not to generic rendering noise.</action>
  <verify>
    <automated>cmake --build build --target test_realtime_scene_factory test_viewer_four_camera_rig test_optix_direction test_optix_equi_path_trace test_viewer_scene_reload -j4 && ctest --test-dir build -R '^(test_realtime_scene_factory|test_viewer_four_camera_rig|test_optix_direction|test_optix_equi_path_trace|test_viewer_scene_reload)$' --output-on-failure</automated>
  </verify>
  <acceptance_criteria>The combined regression slice for scene factory, viewer rig, OptiX camera execution, and viewer reload passes together, and the updated tests all assert the same model/intrinsics/pose preservation contract instead of mixing pinhole-only and model-aware assumptions.</acceptance_criteria>
  <done>Phase 3 closes with coherent end-to-end regression coverage for realtime and viewer camera-model preservation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| file-backed authored scene -> runtime/viewer regressions | Authored realtime camera payloads must survive catalog reload and runtime rig creation without pinhole fallback |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | T | file-backed realtime presets | mitigate | Add temp YAML equi preset coverage to the factory and viewer reload tests so authored camera models are preserved through the live catalog path |
| T-03-03-02 | R | regression slice coherence | mitigate | Run factory, viewer rig, OptiX, and reload regressions together so stale pinhole-only assumptions are caught before verify-work |
</threat_model>

<verification>
Build and run the combined Phase 3 regression slice across realtime factory, viewer rig, OptiX camera execution, and viewer reload.
</verification>

<success_criteria>
File-backed realtime presets, viewer reload, and four-camera mixed-model regressions all preserve the same realtime/viewer camera contract after the Phase 3 implementation lands.
</success_criteria>

<output>
After completion, create `.planning/phases/03-realtime-gpu-and-viewer-camera-models/03-03-SUMMARY.md`
</output>
